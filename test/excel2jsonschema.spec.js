var assert = require('assert')

describe('The excel-2-jsonschema', function() {
  describe('when init project', function() {
    it('should be true', function() {
      assert.equal('true', 'true')
    })
  })
})
